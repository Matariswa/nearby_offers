rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Security rules will be implemented with authentication.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
